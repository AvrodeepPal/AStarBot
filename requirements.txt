# Thin shim for platforms that only read requirements.txt (Streamlit Community
# Cloud, the devcontainer). Dependencies are declared once in pyproject.toml.
# The extra index makes pip prefer CPU-only torch wheels (no multi-GB CUDA libs).
--extra-index-url https://download.pytorch.org/whl/cpu
-e .
